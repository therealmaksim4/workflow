global = {
    show_command_name = true, -- Shows the name of the command being ran
}

cheat = {
    type_full_url = false, -- If true "URL: (language/query or util~query)". Which means you get more freedom.
}

help = {
    seperator = "-", -- What seperates the command and the description
}

command = {
    show_command_name = true, -- Shows the name of the shell command being ran
}

function on_command_start() -- When running a command this function will be ran at the start
    -- Empty
end
